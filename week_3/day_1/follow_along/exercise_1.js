/* eslint-disable max-len */
// ternary , condition ? return if condition true : return if condition false
// and an arrow function
const checkAge = (year) => year > 2000 ? console.log('You are in the 21st century') : console.log('You live in the Middle Age');

checkAge(2001);

